'use client';

/**
 * Placeholder for extended tabbed module content (split from ModuleTabs when needed).
 * Not wired into the dashboard by default.
 */
export default function ModuleTabsContinued() {
  return null;
}
